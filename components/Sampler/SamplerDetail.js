import React  from "react";
import { View, StyleSheet } from "react-native";

const SamplerDetail = () => {
  return (
      <View style={styles.container} />
  );
};


const styles = StyleSheet.create({
  container : {
    backgroundColor: "#03224c", 
    borderWidth: 2,
    margin: "2vw", 
    height: "25vw", 
    width: "25vw" 
  }
})

export default SamplerDetail;
